#include <stdio.h>



int main(void) {
    char sequence[200];
    scanf("%s", sequence);
    int i = 0;
    while (sequence[i] !='\0') {
        switch (sequence[i]) {
            case 'S':
                
        }
    }
    return 0;
}
